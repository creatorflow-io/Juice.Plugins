name: Pack alpha packages

on:
  push:
    branches:
      - 'master'
      - 'main'
      - 'release/*'
    paths-ignore:
      - '*.md'
  workflow_dispatch:

jobs:
  build:
    uses: creatorflow-io/shared-workflows/.github/workflows/shared-pack.yml@net8
    with:
       Stage: alpha
    secrets: inherit